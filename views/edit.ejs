<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <p>Edit the Chat :<%= chat._id %></p>
    <p>This chat is sent by <b><%=chat.from%> </b> <b><%= chat.to %></b></p>
    <form method="post" action="/chats/<%= chat._id %>?_method=put">
        <textarea name="message"  cols="50" rows="6" ><%= chat.message %></textarea>
        <br>
        <button>Edit</button>

    </form>
</body>
</html>